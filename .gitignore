# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test artifacts
.coverage

# Secrets and runtime state
.env
logs/

# data/state.json is intentionally tracked - GitHub Actions runners are
# ephemeral, so send-history has to be committed back to persist across runs.
